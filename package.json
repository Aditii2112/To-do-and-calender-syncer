{
  "name": "oasis",
  "private": true,
  "scripts": {
    "dev": "concurrently \"uvicorn api.main:api --reload --host 0.0.0.0 --port 8000\" \"cd oasis-frontend && npm run dev\"",
    "dev:backend": "uvicorn api.main:api --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd oasis-frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
