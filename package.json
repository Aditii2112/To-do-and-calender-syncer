{
  "name": "oasis",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && source venv/bin/activate && uvicorn api.main:api --reload --host 0.0.0.0 --port 8000\" \"cd frontend && npm run dev\"",
    "dev:backend": "cd backend && source venv/bin/activate && uvicorn api.main:api --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
